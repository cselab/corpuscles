dih_angle_sub(a, b, c, d):=block([n, k, x, nk, y, e, ang],
  n: tri_normal(b, c, a, n),
  k: tri_normal(c, b, d, k),
  x: vec_dot(n, k),
  nk: vec_cross(n, k),
  y: vec_abs(nk),
  e: c - b, /* TODO: */
  ang);
