load("he/vec.mac") $
load("he/edg.mac") $
load("he/dedg.mac") $
load("he/tri.mac") $
load("he/dtri.mac") $

set_random_state(make_random_state(42));
vec_any(a); vec_any(u);
vec_any(b); vec_any(c);
vec_any(v); vec_any(w);

[du, dv, dw]: dtri_beta(a, b, c, u, v, w), numer;

du + dv + dw;
vec_cross(u, du) + vec_cross(v, dv) + vec_cross(w, dw);

vec_arg(a, b, c, u, v, w);