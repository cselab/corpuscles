int dedg2_abs(const real[2], const real[2], /**/ real[2], real[2]);
int dedg2_sq(const real[2], const real[2], /**/ real[2], real[2]);
int dedg2_alpha(const real[2], const real[2], const real[2], const real[2],
                real[2], real[2]);
int dedg2_strain(real Ka, real a3, real a4, const real[2], const real[2],
                 const real[2], const real[2], real[2], real[2]);
