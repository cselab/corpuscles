load("scifac") $
load("to_poly_solve") $

e: [l1 = sqrt(2*e1 + 1),
    l2 = sqrt(2*e2 + 1),
    I1 = e1 + e2,
    I2 = e1*e2,
    a =  l1*l2 - 1,
    b = (l1 - l2)^2/(2*l1*l2)];

gcfac(solve(e, [e1, e2, l1, l2, a, b]));
