. co.util

prog=he.path

usg () {
    msg "$prog returns he path"
    exit
}

if test $# -ne 0 && test "$1" = -h; then usg; fi

path="%path%"

if ! test -d "$path"
then msg "no '$path' dierecoty"
fi

echo "$path"
