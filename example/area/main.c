#include <stdio.h>
#include <real.h>
#include <he/err.h>
#include <he/x.h>

void main0() {
    int m, i, j, k;
    real At, a[3], b[3], c[3];

    for (m = 0; m < NT; m++) {
        i = T0[i]; j = T1[i]; k = T2[i];
        vec_ini(
    }
    
}

int main() {
    ini("/dev/stdin");
    main0();
    fin();
    return HE_OK;
}
